import { getCurrentUser } from "./user-services";
import { login, logout, register } from "./auth-services";
import { getProducts } from "./product-servicess";

export { getCurrentUser, login, logout, register, getProducts };
